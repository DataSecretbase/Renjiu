<view class="container">
    <view class="address-list">
        <view class="a-address"  wx:for-items="{{addressList}}" wx:key="{{index}}">
             <view class="left-text {{item.fields.is_default? 'active':''}}" bindtap="selectTap" data-id="{{item.pk}}">
                 <view class="name-tel">
                    {{item.fields.link_man}}  {{item.fields.mobile}}
                 </view>
                 <view class="address-box">
                    {{item.fields.address}}
                 </view>     
             </view>   
             <view class="right-edit" bindtap="editAddess" data-id="{{item.pk}}"></view> 
        </view>
    </view>
    <view class="bottom-box">
        <view class="add-btn" bindtap="addAddess">新增收货地址</view>
    </view>

</view>